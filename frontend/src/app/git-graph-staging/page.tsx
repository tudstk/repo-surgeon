import WorkspacePlaceholder from '../workspace-placeholder';

export default function GitGraphStagingPage() {
  return <WorkspacePlaceholder title="Git Graph & Staging" path="/git-graph-staging" />;
}
