# Repository safety fixture

This test-only repository-shaped directory is the adversarial input for Milestone 1.
It contains only fake data and may be registered as a test repository.

Run `python generate_fixtures.py` from this directory to regenerate the deterministic binary and oversized files.
The generated files are committed so a test run never needs generation privileges.
